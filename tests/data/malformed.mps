this file claims to be MPS but holds no MPS section headers
it stands in for a truncated or corrupted instance: every line
falls outside a known section, so the reader skips all of it and
produces a 0x0 model.
